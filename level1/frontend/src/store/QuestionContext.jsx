import { createContext } from "react";

// Create a context
const QuestionContext = createContext(); // You can pass a defaultValue parameter for better autocompletion inside your editor/IDE
export default QuestionContext;
